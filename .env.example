NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Catatan Keamanan:
# - NEXT_PUBLIC_* variables AKAN di-expose ke browser
# - Gunakan hanya API keys dengan limited scope (anonymous key)
# - Jangan expose service_role_key atau admin keys di public
# - Selalu enable RLS (Row Level Security) di Supabase
# - Verifikasi authentication di middleware dan pages
# - Validasi semua user inputs dengan schemas (Zod)